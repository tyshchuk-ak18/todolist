#!groovy

pipeline {
    agent any
    options{
        disableConcurrentBuilds()
        pipelineTriggers([pollSCM('H/15 * * * *')])
        timestapse()
    }
    stages {
        stage("Simple test 8") {
            steps {
                bat 'cd'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
