#!groovy

pipeline {
    agent any
    options{
        disableConcurrentBuilds()
        triggers([pollSCM('H/5 * * * *')])
        timestaps()
    }
    stages {
        stage("Simple test") {
            steps {
                bat 'cd'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
