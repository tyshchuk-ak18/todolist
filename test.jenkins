#!groovy

pipeline {
    agent any

    stages {
        stage("Simple test") {
            steps {
                bat 'dir'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
