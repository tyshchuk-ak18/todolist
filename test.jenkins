#!groovy

pipeline {
    agent any

    stages {
        stage('Prepare packages') {
            steps {
                bat 'npm i'
            }
        }
        stage('Build and Test') {
            steps {
                bat 'npm run ng test'
            }
        }
    }
}
