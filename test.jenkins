#!groovy

pipeline {
    agent any
    options{
        disableConcurrentBuilds()
        pipelineTriggers([pollSCM('H/5 * * * *')])
        timestapse()
    }
    stages {
        stage("Simple test 9") {
            steps {
                bat 'cd'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
