#!groovy

pipeline {
    agent any

    stages {
        stage("Test") {
            steps{
                bat 'npm test'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
