#!groovy
properties([disableConcurrentBuilts()])

pipeline {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '4',artifactNumToKeepStr: '4'))
        timestamps()
    }
    stages {
        stage("Simple test 1") {
            steps {
                bat 'java -vesrion'
            }
        }
    }
}
