#!groovy

pipeline {
    agent any

    stages {
        stage("Init") {
            steps {
                bat 'ng test'
            }
        }
        stage("Test") {
            steps{
                bat 'test.bat'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
