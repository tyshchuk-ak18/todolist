#!groovy

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/tyshchuk-ak18/todolist.git'
            }
        }
        stage('Test') {
            steps{
                bat 'npm i'
                bat 'npm run ng test'
            }
        }
    }
}
