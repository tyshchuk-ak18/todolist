#!groovy

pipeline {
    agent any

    stages {
        stage("Simple test") {
            steps {
                bat 'ng test'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
