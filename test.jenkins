#!groovy

pipeline {
    agent any
    options{
        disableConcurrentBuilds()
        timestamps()
    }
    triggers{
        pollSCM('H/5 * * * *')
    }
    stages {
        stage("Simple test") {
            steps {
                bat 'ng'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
