#!groovy

pipeline {
    agent any

    stages {
        stage("Test") {
            steps{
                bat 'npm'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
