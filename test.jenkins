#!groovy

pipeline {
    agent any

    stages {
        stage("Simple test") {
            steps {
                bat 'cd todolist'
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}
