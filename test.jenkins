#!groovy

pipeline {
    agent any

    stages {
        stage('Prepare packages') {
            steps {
                bat 'npm i'
            }
        }
        stage('Build and Test') {
            steps {
                //bat 'npm run ng test'
                echo 'Hallo Welt'
            }
        }
    }

    post {
        always {
            emailext attachLog: true, body: '', subject: '', to: 'tyshchuk_ak18@nuwm.edu.ua'
        }
    }
}
