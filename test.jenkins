#!groovy

pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
    }
    options {
        disableConcurrentBuilds()
        buildDiscarder(logRotator(numToKeepStr: '4',artifactNumToKeepStr: '4'))
        timestamps()
    }
    stages {
        stage("Simple test 6") {
            steps {
                bat 'cd'
            }
        }
    }
    post {
        always {
            
        }
    }
}
