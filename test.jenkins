#!groovy

pipeline {
    agent {
        label 'master'
    }
    options {
        disableConcurrentBuilds()
        buildDiscarder(logRotator(numToKeepStr: '4',artifactNumToKeepStr: '4'))
        timestamps()
    }
    stages {
        stage("Simple test 2") {
            steps {
                bat 'java -vesrion'
            }
        }
    }
}
