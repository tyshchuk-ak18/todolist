#!groovy

pipeline {
    agent {
        label 'master'
    }
    triggers {
        pollSCM('****')
    }
    options {
        disableConcurrentBuilds()
        buildDiscarder(logRotator(numToKeepStr: '4',artifactNumToKeepStr: '4'))
        timestamps()
    }
    stages {
        stage("Simple test 5") {
            steps {
                bat 'cd'
            }
        }
    }
}
