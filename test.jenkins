#!groovy

pipeline {
    agent any

    stages {
        stage("prepare") {
            steps{
                bat 'npm install -g "@angular/cli"'
            }
        }
        stage("Test") {
            steps{
                bat 'npm test'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
