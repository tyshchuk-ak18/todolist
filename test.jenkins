#!groovy

pipeline {
    agent any

    stages {
        stage("Init") {
            steps {
                bat 'npm instal'
            }
        }
        stage("Test") {
            steps{
                bat 'test.bat'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
